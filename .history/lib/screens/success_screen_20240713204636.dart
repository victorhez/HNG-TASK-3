import 'package:flutter/material.dart';

class SucessScreen extends StatelessWidget {
  const SucessScreen({super.key});
   static const String routeName = '/succes';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}